import { motion } from 'motion/react';
import { ArrowUpRight } from 'lucide-react';

export default function BookingCta({ onBook }: { onBook: () => void }) {
  return <section className="relative isolate overflow-hidden px-6 py-28 md:px-12"><img src="https://images.unsplash.com/photo-1519741497674-611481863552?auto=format&fit=crop&w=2200&q=85" alt="Couple celebrating at their wedding" className="absolute inset-0 -z-20 h-full w-full object-cover" loading="lazy" /><div className="absolute inset-0 -z-10 bg-gradient-to-r from-black via-black/75 to-black/20" /><motion.div initial={{ opacity: 0, y: 24 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="mx-auto max-w-7xl"><span className="eyebrow text-gold-400">Now booking 2026—27</span><h2 className="mt-5 max-w-2xl font-serif text-5xl leading-[.95] text-white md:text-7xl">Let’s capture <em className="text-gold-400">your story.</em></h2><p className="mt-6 max-w-md text-sm leading-7 text-white/70">Tell us about the moment, the people and the feeling you want to remember. We’ll shape the rest with care.</p><motion.button whileHover={{ scale: 1.04 }} whileTap={{ scale: .97 }} onClick={onBook} className="mt-9 inline-flex items-center gap-3 rounded-full bg-gold-400 px-6 py-4 text-[11px] font-bold tracking-[.14em] text-black">BOOK YOUR SESSION <ArrowUpRight className="h-4 w-4" /></motion.button></motion.div></section>;
}
